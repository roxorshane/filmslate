
  # High-Fidelity FilmSlate Prototype

  This is a code bundle for High-Fidelity FilmSlate Prototype. The original project is available at https://www.figma.com/design/ALFsdRGDLvBM3mkMYU0klI/High-Fidelity-FilmSlate-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  